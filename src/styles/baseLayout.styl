@import './public'
.container {
    width: 90%;
    min-width: 1170px;
    margin 20px auto 100px auto
    > .content {
        > .main {
            padding: 20px 20px 70px 20px;
            > .main-header {
                @extend .flex-between
            }
        }
        > .sidebar {
            padding-left: 30px
        }
    }
}